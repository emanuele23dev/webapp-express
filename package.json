{
  "name": "52-webapp-express",
  "version": "1.0.0",
  "description": "Il nostro obbiettivo é creare una semplice app gestionale per inserire `film` e `recensioni pubbliche`.",
  "main": "app.js",
  "scripts": {
    "dev": "node --watch --env-file=.env app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mysql2": "^3.11.5"
  }
}
